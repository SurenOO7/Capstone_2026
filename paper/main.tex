\documentclass[12pt,a4paper]{report}

% --- Font & language ---
\usepackage{fontspec}
\setmainfont{Sylfaen}[
  Path=/Users/surenarzumanyan/Library/Fonts/,
  Extension=.ttf,
  UprightFont=sylfaen,
  AutoFakeBold=2.2
]
\usepackage{polyglossia}
\setmainlanguage{armenian}
\setotherlanguage{english}
\addto\captionsarmenian{\renewcommand{\contentsname}{ԲՈՎԱՆԴԱԿՈՒԹՅՈՒՆ}}
% Force Arabic numerals for all counters (polyglossia overrides to Armenian alphabet by default)
\renewcommand{\thefigure}{\thechapter.\number\value{figure}}
\renewcommand{\thetable}{\thechapter.\number\value{table}}
\renewcommand{\theequation}{\thechapter.\number\value{equation}}
\renewcommand{\thefootnote}{\number\value{footnote}}
% Sylfaen supports Armenian Unicode but polyglossia needs explicit declaration
\newfontfamily\armenianfont{Sylfaen}[
  Script=Armenian,
  Path=/Users/surenarzumanyan/Library/Fonts/,
  Extension=.ttf,
  UprightFont=sylfaen,
  AutoFakeBold=2.2
]
\newfontfamily\armenianfonttt{Sylfaen}[
  Script=Armenian,
  Path=/Users/surenarzumanyan/Library/Fonts/,
  Extension=.ttf,
  UprightFont=sylfaen,
  AutoFakeBold=2.2
]

% --- Page geometry ---
\usepackage[a4paper, left=3cm, right=1cm, top=2.5cm, bottom=2cm]{geometry}

% --- Spacing ---
\usepackage{setspace}
\setstretch{1.5}
\setlength{\emergencystretch}{3em}

% --- Page numbers bottom-right, no header ---
\usepackage{fancyhdr}
\newcommand{\usedigitpagenumbers}{\renewcommand{\thepage}{\number\value{page}}}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[R]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[R]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% --- Chapter/section titles: UPPERCASE, Bold, 12pt, no trailing period ---
\usepackage{titlesec}
\usepackage{tocloft}
\setcounter{secnumdepth}{1}
\setcounter{tocdepth}{1}
\renewcommand{\thechapter}{\number\value{chapter}}
\renewcommand{\thesection}{\thechapter.\number\value{section}}
\makeatletter
\renewcommand{\@pnumwidth}{2.5em}
\renewcommand{\@tocrmarg}{3.5em}
\makeatother
\titleformat{\chapter}[block]
  {\normalfont\fontsize{12}{14}\selectfont\bfseries}
  {ԳԼՈՒԽ \thechapter.}{0.5em}{\MakeUppercase}
\titleformat{name=\chapter,numberless}[block]
  {\normalfont\fontsize{12}{14}\selectfont\bfseries}
  {}{0pt}{\MakeUppercase}
\titleformat{\section}[block]
  {\normalfont\fontsize{12}{14}\selectfont\bfseries}
  {\thesection}{1em}{}
\titlespacing*{\chapter}{0pt}{0pt}{12pt}
\renewcommand{\cftchappresnum}{ԳԼՈՒԽ }
\renewcommand{\cftchapaftersnum}{.}
\setlength{\cftchapnumwidth}{5.5em}
\setlength{\cftsecnumwidth}{3em}
% Each new chapter starts on a new page
\newcommand{\chapterbreak}{\clearpage}

% --- Bibliography (APA via biblatex) ---
\usepackage[backend=biber, style=apa, sorting=nyt]{biblatex}
\addbibresource{references.bib}

% --- Utilities ---
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\hypersetup{hidelinks}

% --- Footnotes ---
\usepackage{footmisc}

\begin{document}

% Title page (no page number)
\pagenumbering{gobble}
\input{chapters/titlepage}

% Table of contents
\pagenumbering{arabic}
\usedigitpagenumbers
\begingroup
\sloppy
\tableofcontents
\endgroup
\clearpage

% Main content — Arabic page numbering from Introduction
\pagenumbering{arabic}
\usedigitpagenumbers
\input{chapters/introduction}
\input{chapters/literature}
\input{chapters/methods}
\input{chapters/results}
\input{chapters/conclusions}

% Bibliography
\clearpage
\printbibliography[title={ՕԳՏԱԳՈՐԾՎԱԾ ԳՐԱԿԱՆՈՒԹՅԱՆ ՑԱՆԿ}]

% Appendices
\input{appendices/app_screenshots}
\input{appendices/design_artifacts}

\end{document}
